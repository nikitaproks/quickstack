# {{ project_name }}

## Dependencies
- [Docker](https://docs.docker.com/engine/install/) - A platform for building, sharing, and running applications with containers.
- [Docker Compose](https://docs.docker.com/compose/install/) - A tool for defining and running multi-container Docker applications.
- [Make](https://www.gnu.org/software/make/) - A build automation tool that automatically builds executable programs and libraries from source code by reading files called Makefiles which specify how to derive the target program.
{% if backend == 'FastAPI' -%}
- [UV](https://docs.astral.sh/uv/getting-started/installation/) - An extremely fast Python package and project manager, written in Rust.
{%- endif %}

## Environment
Open `.env` file and set the environment variables.

## Setup
{% if backend == 'FastAPI' -%}
### FastAPI
1. `cd server_fastapi`
2. `uv sync`
{%- endif %}

## Local development
From the root directory:
- `make run-dev` to start all services for local development.
- `make stop-dev` to stop all services.

## Testing
From the root directory:
- `make test` to run all tests.
{% if backend == 'FastAPI' -%}
- `make test-server-fastapi` to run tests on FastAPI server.
{%- endif %}
