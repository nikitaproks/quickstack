[project]
name = "{{project_name|lower|replace(' ', '_')}}_server_fastapi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    # TODO: Add authors
]
requires-python = ">={{python_version}}, <{{python_version + 0.1}}"
dependencies = [
    "fastapi[standard]",
]

[dependency-groups]
dev = [
    "pytest",
    "mypy",
    "ruff",
]


[project.scripts]
server = "server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["server"]
